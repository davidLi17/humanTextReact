# 日志级别修复测试指南

## 修复的问题

1. **日志级别设置不生效** - 修复了 `initializeLogger` 函数中的 debug 包启用逻辑
2. **日志过滤逻辑优化** - 改进了 `shouldLog` 函数的级别判断逻辑
3. **调试信息增强** - 添加了详细的调试输出和状态检查

## 测试步骤

### 1. 安装扩展
1. 打开 Chrome 浏览器
2. 进入 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `.output/chrome-mv3` 目录

### 2. 测试日志级别设置
1. 右键点击扩展图标，选择"选项"
2. 在设置页面中，找到"日志级别"设置
3. 尝试不同的日志级别设置：
   - 关闭日志
   - 仅错误
   - 警告及以上
   - 信息及以上
   - 全部日志

### 3. 验证日志输出
1. 打开 Chrome 开发者工具（F12）
2. 切换到 Console 面板
3. 在设置页面修改日志级别并点击"保存设置"
4. 观察控制台输出，应该能看到：
   - `[日志系统] 已初始化，级别: xxx`
   - `[Options] 测试日志级别: xxx`
   - 根据设置的级别显示相应的日志

### 4. 预期结果

#### 当设置为"关闭日志"时：
- 只能看到 `[日志系统] 已初始化，级别: off`
- 不应该看到任何带有 `human-text:` 前缀的日志

#### 当设置为"仅错误"时：
- 应该只能看到 error 级别的日志
- 不应该看到 info、warn、success、trace 等其他级别的日志

#### 当设置为"全部日志"时：
- 应该能看到所有级别的日志输出

## 调试信息

修复后的代码会输出以下调试信息：

```
[日志系统] 已初始化，级别: debug
[Options] 当前日志级别: debug
[Options] localStorage debug 设置: human-text:*
[Options] 测试日志级别: debug
⚙️ ℹ️ 这是一条 info 日志
⚙️ ⚠️ 这是一条 warn 日志
⚙️ ❌ 这是一条 error 日志
⚙️ ✅ 这是一条 success 日志
```

## 常见问题

如果仍然看不到日志输出，请检查：

1. **控制台过滤器**：确保没有设置过滤条件
2. **日志级别**：确认设置的级别不是"关闭日志"
3. **浏览器权限**：确保扩展有足够的权限
4. **缓存问题**：尝试重新加载扩展页面

## 技术细节

### 修复的关键代码：

1. **`initializeLogger` 函数**：
   - 使用 `debugLib.enable()` 替代手动设置 `debugLib.enabled`
   - 使用 `debugLib.disable()` 完全禁用 debug
   - 添加初始化状态的调试输出

2. **`shouldLog` 函数**：
   - 优化了日志级别的判断逻辑
   - 确保所有日志类型都能正确过滤

3. **设置页面**：
   - 添加了日志级别测试功能
   - 增加了状态检查和调试输出
   - 确保保存后重新初始化日志系统

这个修复应该能够彻底解决日志级别设置不生效的问题。