# 个人阅读演进进度

## 目标与交付约定

以个人每天使用顺手为目标。用户已恢复 Windows 开发，继续使用 Bun、main 和现有 GitHub CI/CD 交付；此前的暂停及 Mac 交接已结束。

固定队列：

1. 当前段落上下文默认开启，可在设置关闭；点击翻译或快捷键后立即生成，省去逐次确认。
2. 按当前目标提取网页重点，并提供可回查的原文依据。
3. 对话主线 recap：当前目标、已确认结论、未解决问题、下一步。

每项完成对应测试、构建、提交、推送和 CI 验收后再进入下一项。最终统一更新扩展版本并沿用版本标签发布。

## 已有基础

- 一键定向补讲已提交到 main：`8e950de`。
- 对应 CI：<https://github.com/davidLi17/humanTextReact/actions/runs/34704680866>，结果 success。
- 段落上下文复用 `contextualSelectionEnabled`；新安装及缺省设置默认开启，已有明确关闭设置继续保留。
- 自动翻译仍保留可查看的段落来源；段落提取失败时回退到选中文字翻译。
- 恢复审查发现补讲元数据需校验消息角色及历史引用关系，此项在进入第 2 项前修复。

## 验收状态

段落直译与补讲元数据修复已通过本地验证：快速测试 432 项、全部组件 42 项、编译及 Chrome 构建通过。6 条 E2E 场景均通过；其中收藏来源断言按完整段落更新后单独复验通过。构建体积 954.1 kB，版本暂为 1.6.0。

补讲修复提交为 `56a86f1`。段落直译提交后等待 CI，再开始第 2 项；第 3 项继续排在第 2 项交付之后。

本地原有 `docs/diagrams/` 继续保留。构建产物不进入版本控制。
